{
  "name": "sample",
  "version": "1.0.1",
  "description": "Sample module using the ui-harness.",
  "main": "./lib/index.js",
  "typings": "./lib/index.d.ts",
  "scripts": {
    "start": "node .",
    "watch": "nodemon . -e js --ignore src",
    "lint": "sh ./node_modules/@tdb/typescript/sh/lint.sh",
    "build": "rm -rf ./lib && tsc",
    "reset": "rm -rf ./lib && rm -rf ./build && yarn build",
    "prepublish": "yarn run lint && yarn run build"
  },
  "dependencies": {
    "ui-harness": "4.0.0-alpha.7"
  },
  "devDependencies": {
    "@tdb/typescript": "^2.2.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/philcockfield/ui-harness"
  },
  "license": "UNLICENSED",
  "private": true
}
